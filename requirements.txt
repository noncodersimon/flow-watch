# Runtime dependency for the adapters. The front end has none - vanilla JS
# plus ECharts from a CDN, no npm, no build step.
#
# Needed by adapters/price_volume_yahoo.py and adapters/etf_flows.py. The
# COT, RNS and SEC adapters use urllib from the standard library. Kept
# unpinned to match the workflow, which installs the current release on
# every run.
yfinance
